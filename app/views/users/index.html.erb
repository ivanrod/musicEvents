<h2>Users:</h2>
<ul>
<% @users.each do |user| %>
	<li>
		<b>User: </b><%= link_to(user.name + " - " + user.email, user_path(user)) %> 
	</li>
<% end %>
</ul>
<%= link_to("New", new_user_path) %>

<% if flash[:notice] %>
   <p><%= flash[:notice] %></p> 
<% end %>